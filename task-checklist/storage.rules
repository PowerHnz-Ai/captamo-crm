rules_version = '2';

// Deploy: firebase deploy --only storage (a partir de task-checklist/)
service firebase.storage {
  match /b/{bucket}/o {
    match /companies/{companyId}/{allPaths=**} {
      allow read, write: if false;
    }
  }
}
